create table `custom_field_attributes` (`field_id` INT NOT NULL, 
                                        `name` VARCHAR(255) NOT NULL, 
                                        `value` VARCHAR(255) NOT NULL)