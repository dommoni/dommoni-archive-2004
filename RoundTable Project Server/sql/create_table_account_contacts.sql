create table `account_contacts` (`account_id` INT NOT NULL, 
                                 `phone_number` VARCHAR(255),
   				 `office_address` VARCHAR(255),
                                 `city` VARCHAR(255),
                                 `state` VARCHAR(255),
                                 `zip` VARCHAR(255))