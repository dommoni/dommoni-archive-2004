create table `meeting_followup_items` (`meeting_id` INT NOT NULL,
                                       `title` VARCHAR(255) NOT NULL,
                                       `text` TEXT, 
                                       `ordinal` INT NOT NULL)